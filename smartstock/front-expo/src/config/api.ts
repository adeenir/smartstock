// Update this base URL to point to your backend.
// For Android emulator running on the same machine use 10.0.2.2 which maps to host localhost.
// If you're testing on a physical device, use your machine LAN IP (e.g. 192.168.x.y).
// Backend project is in the `back` folder of this workspace — ensure it's running (npm start or node server) on port 3000.
export const API_BASE_URL = 'http://10.0.2.2:3000';
